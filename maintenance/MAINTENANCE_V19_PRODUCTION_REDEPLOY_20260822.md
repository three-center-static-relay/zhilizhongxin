Production deployment trigger only. No maintenance business logic changes. Purpose: force the current main maintenance-worker source, which already contains the post-273 v19 runtime canary and metadata-only route quarantine logic, through the normal main deployment gate so Cloudflare production runtime catches up with Git main. The deployment gate restores the same current-main baseline after its bounded Phase-2 verification.