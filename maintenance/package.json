{
  "name": "maintenance-worker",
  "private": true,
  "scripts": {
    "cf:build": "node --check src/index.js",
    "cf:preview": "npm run cf:build && wrangler versions upload",
    "cf:deploy": "npm run cf:build && wrangler deploy"
  },
  "devDependencies": {"wrangler": "^4.0.0"}
}
