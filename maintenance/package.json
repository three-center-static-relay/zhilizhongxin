{
  "name": "maintenance-worker",
  "description": "Governance maintenance and adaptive expert-route lifecycle",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:build": "node --check src/index.js && node --check src/expert-route-manager.js && node --check scripts/run-immediate-refresh.mjs",
    "cf:ci:preview": "node ../scripts/cloudflare-worker-gate.mjs maintenance preview && npx --yes wrangler@4.123.0 deploy",
    "cf:ci:deploy": "node ../scripts/cloudflare-worker-gate.mjs maintenance deploy",
    "cf:preview": "npm run cf:build && npx wrangler deploy --dry-run",
    "cf:deploy": "npm run cf:build && npx wrangler deploy"
  },
  "devDependencies": {"wrangler": "4.123.0"}
}
