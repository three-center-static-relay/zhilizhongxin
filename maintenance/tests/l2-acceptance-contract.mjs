import assert from "node:assert/strict";
import fs from "node:fs";
const wrapperSource=fs.readFileSync(new URL("../scripts/run-l2-candidate-acceptance.mjs",import.meta.url),"utf8");
const coreSource=fs.readFileSync(new URL("../scripts/run-l2-candidate-acceptance-core.mjs",import.meta.url),"utf8");
const trigger=JSON.parse(fs.readFileSync(new URL("../l2-acceptance-request.json",import.meta.url),"utf8"));
assert.match(wrapperSource,/L2_STATELESS_VERSION_PREVIEW_PROBE_MODE|L2_STATELESS_SCAFFOLD/);
assert.match(coreSource,/L2_STATELESS_SCAFFOLD_DEPLOY_PROBE_PASS/);
assert.match(coreSource,/scaffold-deploy-begin/);
assert.match(coreSource,/"deploy","--config",configPath/);
assert.match(coreSource,/workers_dev:false/);
assert.match(coreSource,/preview_urls:false/);
assert.match(coreSource,/routes_configured:false/);
assert.match(coreSource,/durable_object_implemented:false/);
assert.match(coreSource,/secret_used:false/);
assert.match(coreSource,/version_upload_attempted:false/);
assert.match(coreSource,/preview_http_attempted:false/);
assert.match(coreSource,/cleanup_required_for_probe_pass:false/);
assert.match(coreSource,/"delete","--name",workerName/);
assert.doesNotMatch(coreSource,/"delete","--name",workerName,"--force"/);
assert.doesNotMatch(coreSource,/"versions","upload"/);
assert.doesNotMatch(coreSource,/"versions","deploy"/);
assert.doesNotMatch(coreSource,/CLOUDFLARE_AI_GATEWAY_API_TOKEN/);
assert.doesNotMatch(coreSource,/ADMIN_GPT_TOKEN/);
assert.doesNotMatch(coreSource,/durable_objects/);
assert.equal(trigger.schema,"expert-l2-acceptance-v1");
assert.equal(trigger.enabled,true);
console.log(JSON.stringify({ok:true,suite:"l2-stateless-scaffold-deploy-probe-contract",scaffold_deploy_only:true,workers_dev_disabled:true,preview_urls_disabled:true,no_routes:true,no_secrets:true,no_durable_objects:true,version_upload_disabled:true,preview_http_disabled:true,best_effort_cleanup_nonblocking:true,existing_production_workers_unchanged:true}));
