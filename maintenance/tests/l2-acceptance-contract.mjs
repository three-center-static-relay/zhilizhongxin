import assert from "node:assert/strict";
import fs from "node:fs";
const wrapperSource=fs.readFileSync(new URL("../scripts/run-l2-candidate-acceptance.mjs",import.meta.url),"utf8");
const coreSource=fs.readFileSync(new URL("../scripts/run-l2-candidate-acceptance-core.mjs",import.meta.url),"utf8");
const trigger=JSON.parse(fs.readFileSync(new URL("../l2-acceptance-request.json",import.meta.url),"utf8"));
assert.match(wrapperSource,/L2_LOCAL_REMOTE_SERVICE_BINDING_MODE/);
assert.match(wrapperSource,/local_worker_execution:true/);
assert.match(wrapperSource,/new_worker_creation:false/);
assert.match(coreSource,/remote:true/);
assert.match(coreSource,/binding:"MAINTENANCE",service:"maintenance-worker",remote:true/);
assert.match(coreSource,/wrangler@\$\{WRANGLER\}`,"dev"/);
assert.match(coreSource,/127\.0\.0\.1/);
assert.match(coreSource,/https:\/\/maintenance\.internal\/health/);
assert.match(coreSource,/L2_LOCAL_REMOTE_SERVICE_BINDING_PROBE_PASS/);
assert.match(coreSource,/new_worker_created:false/);
assert.match(coreSource,/remote_dev_used:false/);
assert.match(coreSource,/versions_uploaded:false/);
assert.match(coreSource,/deployment_mutated:false/);
assert.match(coreSource,/production_traffic_changed:false/);
assert.match(coreSource,/secret_used:false/);
assert.match(coreSource,/ai_gateway_called:false/);
assert.match(coreSource,/dynamic_routes_mutated:false/);
assert.doesNotMatch(coreSource,/"--remote"/);
assert.doesNotMatch(coreSource,/"--local"/);
assert.doesNotMatch(coreSource,/"versions","upload"/);
assert.doesNotMatch(coreSource,/"versions","deploy"/);
assert.doesNotMatch(coreSource,/"deploy"/);
assert.doesNotMatch(coreSource,/CLOUDFLARE_AI_GATEWAY_API_TOKEN/);
assert.doesNotMatch(coreSource,/ADMIN_GPT_TOKEN/);
assert.equal(trigger.schema,"expert-l2-acceptance-v1");
assert.equal(trigger.enabled,true);
console.log(JSON.stringify({ok:true,suite:"l2-local-remote-service-binding-probe-contract",local_execution:true,remote_service_binding:true,target:"maintenance-worker",health_only:true,no_worker_creation:true,no_remote_dev:true,no_version_upload:true,no_deployment_mutation:true,no_secrets:true,no_ai_gateway:true,no_dynamic_routes:true}));
