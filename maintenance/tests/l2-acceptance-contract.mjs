import assert from "node:assert/strict";
import fs from "node:fs";
const wrapperSource=fs.readFileSync(new URL("../scripts/run-l2-candidate-acceptance.mjs",import.meta.url),"utf8");
const coreSource=fs.readFileSync(new URL("../scripts/run-l2-candidate-acceptance-core.mjs",import.meta.url),"utf8");
const trigger=JSON.parse(fs.readFileSync(new URL("../l2-acceptance-request.json",import.meta.url),"utf8"));
assert.match(wrapperSource,/L2_LOCAL_ROUTE_CANDIDATE_MODE/);
assert.match(wrapperSource,/worker_deployment_mutation:false/);
assert.match(coreSource,/readFileSync\(resolve\("src\/expert-route-manager\.js"\)/);
assert.match(coreSource,/binding:"EXPERT_CENTER",service:"expert-worker",remote:true/);
assert.match(coreSource,/wrangler@\$\{WRANGLER\}`,"dev"/);
assert.match(coreSource,/CLOUDFLARE_AI_GATEWAY_API_TOKEN/);
assert.match(coreSource,/refreshExpertRoute\(env,\{previous:null,expertBinding:env\.EXPERT_CENTER\}\)/);
assert.match(coreSource,/route_family_count:8/);
assert.match(coreSource,/company_lane_count:8/);
assert.match(coreSource,/company_diverse:true/);
assert.match(coreSource,/rollback_rehearsal_ok:true/);
assert.match(coreSource,/dynamic_routes_deployed:true/);
assert.match(coreSource,/worker_deployment_mutated:false/);
assert.match(coreSource,/production_worker_traffic_changed:false/);
assert.match(coreSource,/NO_PREVIOUS_ROUTE_VERSION_FOR_REHEARSAL/);
assert.doesNotMatch(coreSource,/"versions","upload"/);
assert.doesNotMatch(coreSource,/"versions","deploy"/);
assert.doesNotMatch(coreSource,/admin-worker/);
assert.doesNotMatch(coreSource,/ADMIN_GPT_TOKEN/);
assert.equal(trigger.schema,"expert-l2-acceptance-v1");
assert.equal(trigger.enabled,true);
console.log(JSON.stringify({ok:true,suite:"l2-local-route-candidate-contract",candidate_code_local:true,expert_binding_remote:true,worker_deployments_unchanged:true,dynamic_routes_real:true,eight_routes_required:true,eight_companies_required:true,selftest_required:true,rollback_rehearsal_required:true,fail_closed:true}));
