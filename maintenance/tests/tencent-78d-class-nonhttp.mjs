import assert from 'node:assert/strict';
const expected='78d8af132074f2ab8edba1001687847e5ee206bc';
const c=new AbortController(),timer=setTimeout(()=>c.abort(),30000);
try{const r=await fetch('https://admin-worker.a15280020511.workers.dev/_internal/tencent-production-attestation',{headers:{accept:'application/json'},signal:c.signal});const b=await r.json().catch(()=>null);const exact=r.status===200&&b?.validation==='FAIL'&&b?.failed_commit===expected&&b?.fail_closed===true&&b?.secret_values_exposed===false;const code=String(b?.failure_code||'');const a=/^(The_|This_|Request_|Operation_|A_|An_)/i.test(code),e=/^(Cannot_|Could_|Failed_|Unable_|Error_|Unsupported_|Not_|No_|Invalid_|Expected_|Missing_)/i.test(code);const matched=exact&&!a&&!e;console.log(JSON.stringify({suite:'residual-prefix-other',matched,secret_values_read:false}));assert.equal(matched,true,'RESIDUAL_PREFIX_OTHER_REQUIRED')}finally{clearTimeout(timer)}
