import assert from "node:assert/strict";
import fs from "node:fs";
const source=fs.readFileSync(new URL("../src/index.js",import.meta.url),"utf8");
const config=JSON.parse(fs.readFileSync(new URL("../wrangler.jsonc",import.meta.url),"utf8"));
const binding=(config.services||[]).find(x=>x.binding==="AI_GATEWAY_CONTROL");
assert.deepEqual(binding,{binding:"AI_GATEWAY_CONTROL",service:"admin-worker",entrypoint:"AIGatewayControl"});
assert.match(source,/AI_GATEWAY_CONTROL\.request\(\{operation:"routes\.list"\}\)/);
assert.match(source,/maintenance-ai-gateway-control-v1/);
assert.match(source,/MAINTENANCE_RUNTIME_E2E_PROBE/);
assert.match(source,/x-maintenance-e2e-probe/);
assert.match(source,/constantTimeEqual/);
assert.match(source,/cloudflare_credentials_local:false/);
assert.match(source,/control_plane_owner:"admin-worker"/);
assert.match(source,/dynamic_route_mutation:false/);
assert.match(source,/production_worker_mutated:false/);
assert.match(source,/production_worker_traffic_changed:false/);
assert.doesNotMatch(source,/CLOUDFLARE_AI_GATEWAY_API_TOKEN/);
assert.doesNotMatch(source,/CF_API_TOKEN/);
assert.doesNotMatch(source,/Authorization/);
console.log(JSON.stringify({ok:true,suite:"maintenance-runtime-ai-gateway-control-contract",internal_named_binding:true,read_only_routes_list:true,no_local_cloudflare_token:true,probe_guarded_selftest:true,scheduled_control_plane_health:true,secrets_redacted:true}));
