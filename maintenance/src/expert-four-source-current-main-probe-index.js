import {discoverWorkersAI} from "./model-universe.js";
const json=(body,status=200)=>Response.json(body,{status,headers:{"cache-control":"no-store"}});const clean=v=>String(v??"").trim();const safe=v=>clean(v).replace(/[^0-9A-Za-z_.:/@+-]/g,"_").slice(0,180);
function auth(request,env){const a=String(env.MAINTENANCE_RUNTIME_E2E_PROBE||""),b=String(request.headers.get("x-maintenance-e2e-probe")||"");if(!a||a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}
export default{async fetch(request,env){if(!auth(request,env))return json({ok:false,error:"NOT_FOUND"},404);try{const rows=await discoverWorkersAI(env),companies=[...new Set(rows.map(x=>x.company).filter(Boolean))],validated=rows.filter(x=>x?.meta?.chat_route_validation==="workers-ai-model-schema-messages").length,ok=rows.length>0&&validated===rows.length;return json({ok,stage:"workers-ai-chat-schema-nonempty",candidate_count:rows.length,company_count:companies.length,validated_count:validated,chat_schema_validated:validated===rows.length,secrets_redacted:true},ok?200:502)}catch(error){return json({ok:false,stage:"workers-ai-chat-schema-nonempty",error:safe(error?.message||error),secrets_redacted:true},502)}}};export class MaintenanceState{constructor(state){this.state=state}async fetch(){return json({ok:true,diagnostic_only:true})}}
