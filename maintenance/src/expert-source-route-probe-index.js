const json=(body,status=200)=>Response.json(body,{status,headers:{"cache-control":"no-store"}});
const safe=v=>String(v??"UNKNOWN").replace(/[^0-9A-Za-z_.:/-]/g,"_").slice(0,180);
function auth(request,env){const a=String(env.MAINTENANCE_RUNTIME_E2E_PROBE||""),b=String(request.headers.get("x-maintenance-e2e-probe")||"");if(!a||a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}
export default{async fetch(request,env){if(!auth(request,env))return json({ok:false,error:"NOT_FOUND"},404);const token=String(env.CLOUDFLARE_AI_GATEWAY_API_TOKEN||"").trim();if(!token)return json({ok:false,stage:"replaced-token-active",error:"TOKEN_MISSING",secrets_redacted:true},502);try{const r=await fetch("https://api.cloudflare.com/client/v4/user/tokens/verify",{headers:{authorization:`Bearer ${token}`,accept:"application/json"}}),p=await r.json().catch(()=>null),ok=r.ok&&p?.success===true&&String(p?.result?.status||"")==="active";return json({ok,stage:"replaced-token-active",http_status:r.status,token_status:ok?"active":"invalid",error:ok?null:safe(Array.isArray(p?.errors)&&p.errors[0]?.code!=null?`CF_${p.errors[0].code}`:`HTTP_${r.status}`),secrets_redacted:true},ok?200:502)}catch(e){return json({ok:false,stage:"replaced-token-active",error:safe(e?.message||e),secrets_redacted:true},502)}}};
export class MaintenanceState{constructor(state){this.state=state}async fetch(){return json({ok:true,diagnostic_only:true})}}
