import {refreshExpertRoutes} from "./expert-route-manager.js";
const json=(body,status=200)=>Response.json(body,{status,headers:{"cache-control":"no-store"}});
const safe=v=>String(v??"UNKNOWN").replace(/[^0-9A-Za-z_.:/@+-]/g,"_").slice(0,180);
function auth(request,env){const a=String(env.MAINTENANCE_RUNTIME_E2E_PROBE||""),b=String(request.headers.get("x-maintenance-e2e-probe")||"");if(!a||a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}
function clean(v){return String(v??"").trim()}
function routeShard(lane){const n=Math.trunc(Number(lane));return !Number.isFinite(n)||n<1||n>8?null:n<=2?"lanes-1-2":n<=4?"lanes-3-4":n<=6?"lanes-5-6":"lanes-7-8"}
async function cf(env,path,{method="POST",body}={}){const account=clean(env.CF_ACCOUNT_ID||env.CLOUDFLARE_ACCOUNT_ID),gateway=clean(env.AI_GATEWAY_ID||"test"),token=clean(env.CLOUDFLARE_AI_GATEWAY_API_TOKEN||env.CF_API_TOKEN);if(!account||!gateway||!token)throw new Error("ROUTE_CONTROL_NOT_CONFIGURED");const r=await fetch(`https://api.cloudflare.com/client/v4/accounts/${encodeURIComponent(account)}/ai-gateway/gateways/${encodeURIComponent(gateway)}${path}`,{method,headers:{authorization:`Bearer ${token}`,accept:"application/json",...(body===undefined?{}:{"content-type":"application/json"})},...(body===undefined?{}:{body:JSON.stringify(body)})});const p=await r.json().catch(()=>null);if(!r.ok||p?.success===false)throw new Error(`CF_ROUTE_HTTP_${r.status}`);return p}
async function restore(env,receipt){const out=[];for(const item of [...(receipt?.route_family||[])].reverse()){const id=clean(item?.route_id),previous=clean(item?.previous_version_id),created=item?.created===true;try{if(previous){await cf(env,`/routes/${encodeURIComponent(id)}/deployments`,{body:{version_id:previous}});out.push(true)}else if(created){await cf(env,`/routes/${encodeURIComponent(id)}`,{method:"DELETE"});out.push(true)}else out.push(false)}catch{out.push(false)}}return out.length>0&&out.every(Boolean)}
async function expertComplex(env){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),330000);try{const r=await env.EXPERT_CENTER.fetch(new Request("https://expert.internal/v1/run",{method:"POST",headers:{accept:"application/json","content-type":"application/json"},body:JSON.stringify({task_id:`diag-complex-${crypto.randomUUID()}`,prompt:"Complex quantitative systems acceptance. Analyze whether a hypothetical service should launch under uncertain demand using independent strategy, quantitative, risk and adversarial perspectives. Explicitly compare competing assumptions, stress-test downside scenarios, and identify a decision threshold. Keep the final answer concise and self-contained. Do not use external facts.",task_domain:"quantitative",task_type:"analysis",complexity:"high",reasoning_depth:"deep",cost_priority:"economy",model_count:4,rounds:1,max_tokens:192,timeout_seconds:300,cost_mode:"free-first"}),signal:controller.signal}));const b=await r.json().catch(()=>null),experts=Array.isArray(b?.experts)?b.experts:[],judges=Array.isArray(b?.judges)?b.judges:[],parts=[...experts,...judges],receipts=parts.map(x=>({provider:clean(x?.provider),company:clean(x?.company),lane:Number(x?.meta?.lane||x?.lane||0),route_shard:routeShard(x?.meta?.lane||x?.lane)})),companies=new Set(receipts.map(x=>x.company.toLowerCase()).filter(Boolean)),shards=new Set(receipts.map(x=>x.route_shard).filter(Boolean));const ok=r.status===200&&b?.ok===true&&b?.status==="completed"&&b?.company_diverse===true&&parts.length>=3&&companies.size>=3&&shards.size>=2&&/^[a-f0-9]{64}$/i.test(String(b?.output_digest||""));return{ok,http_status:r.status,status:clean(b?.status),participant_count:parts.length,company_count:companies.size,route_shard_count:shards.size,route_shards:[...shards],providers:[...new Set(receipts.map(x=>x.provider).filter(Boolean))].map(safe),error:safe(b?.error||b?.code||b?.message||"")}}finally{clearTimeout(timer)}}
export default{async fetch(request,env){if(!auth(request,env))return json({ok:false,error:"NOT_FOUND"},404);let receipt=null;try{receipt=await refreshExpertRoutes(env);const routeCount=Array.isArray(receipt?.route_family)?receipt.route_family.length:0;if(receipt?.ok!==true||routeCount!==4||Number(receipt?.company_count)<8)throw new Error("ROUTE_REFRESH_PRECONDITION_FAILED");await new Promise(r=>setTimeout(r,900));const expert=await expertComplex(env),restored=await restore(env,receipt),ok=expert.ok&&restored;return json({ok,stage:"workers-ai-complex-expert",route_count:routeCount,company_count:Number(receipt?.company_count||0),expert,route_restored:restored,secrets_redacted:true},ok?200:502)}catch(e){const restored=receipt?await restore(env,receipt).catch(()=>false):false;return json({ok:false,stage:"workers-ai-complex-expert",error:safe(e?.message||e),route_restored:restored,secrets_redacted:true},502)}}};
export class MaintenanceState{constructor(state){this.state=state}async fetch(){return json({ok:true,diagnostic_only:true})}}
