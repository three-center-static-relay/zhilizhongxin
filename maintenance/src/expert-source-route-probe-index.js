const json=(body,status=200)=>Response.json(body,{status,headers:{"cache-control":"no-store"}});
const safe=v=>String(v??"UNKNOWN").replace(/[^0-9A-Za-z_.:/-]/g,"_").slice(0,180);
function auth(request,env){const a=String(env.MAINTENANCE_RUNTIME_E2E_PROBE||""),b=String(request.headers.get("x-maintenance-e2e-probe")||"");if(!a||a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}
export default{async fetch(request,env){if(!auth(request,env))return json({ok:false,error:"NOT_FOUND"},404);const account=String(env.CF_ACCOUNT_ID||env.CLOUDFLARE_ACCOUNT_ID||"").trim(),gateway=String(env.AI_GATEWAY_ID||"test").trim(),token=String(env.CLOUDFLARE_AI_GATEWAY_API_TOKEN||env.CF_API_TOKEN||"").trim();if(!account||!gateway||!token)return json({ok:false,stage:"ai-gateway-routes-read",error:"MISSING_CONFIG",secrets_redacted:true},502);try{const r=await fetch(`https://api.cloudflare.com/client/v4/accounts/${encodeURIComponent(account)}/ai-gateway/gateways/${encodeURIComponent(gateway)}/routes?per_page=100`,{headers:{authorization:`Bearer ${token}`,accept:"application/json"}}),p=await r.json().catch(()=>null),routes=Array.isArray(p?.result)?p.result:Array.isArray(p?.result?.routes)?p.result.routes:Array.isArray(p?.data?.routes)?p.data.routes:[],ok=r.ok&&p?.success!==false;return json({ok,stage:"ai-gateway-routes-read",http_status:r.status,route_count:routes.length,error:ok?null:safe(Array.isArray(p?.errors)&&p.errors[0]?.code!=null?`CF_${p.errors[0].code}`:`HTTP_${r.status}`),secrets_redacted:true},ok?200:502)}catch(e){return json({ok:false,stage:"ai-gateway-routes-read",error:safe(e?.message||e),secrets_redacted:true},502)}}};
export class MaintenanceState{constructor(state){this.state=state}async fetch(){return json({ok:true,diagnostic_only:true})}}
