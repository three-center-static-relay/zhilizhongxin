const json=(body,status=200)=>Response.json(body,{status,headers:{"cache-control":"no-store"}});
const safe=v=>String(v??"UNKNOWN").replace(/[^0-9A-Za-z_.:/-]/g,"_").slice(0,180);
function auth(request,env){const a=String(env.MAINTENANCE_RUNTIME_E2E_PROBE||""),b=String(request.headers.get("x-maintenance-e2e-probe")||"");if(!a||a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}
export default{async fetch(request,env){if(!auth(request,env))return json({ok:false,error:"NOT_FOUND"},404);const c=new AbortController(),t=setTimeout(()=>c.abort(),20000);try{const r=await fetch("https://openrouter.ai/api/v1/models",{headers:{accept:"application/json","user-agent":"three-center-openrouter-probe/1.0"},signal:c.signal}),p=await r.json().catch(()=>null),count=Array.isArray(p?.data)?p.data.length:0,ok=r.status===200&&count>0;return json({ok,stage:"source-openrouter-raw",http_status:r.status,model_count:count,secrets_redacted:true},ok?200:502)}catch(e){return json({ok:false,stage:"source-openrouter-raw",error:safe(e?.message||e),secrets_redacted:true},502)}finally{clearTimeout(t)}}};
export class MaintenanceState{constructor(state){this.state=state}async fetch(){return json({ok:true,diagnostic_only:true})}}
