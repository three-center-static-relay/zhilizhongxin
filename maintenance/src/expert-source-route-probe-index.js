const json=(body,status=200)=>Response.json(body,{status,headers:{"cache-control":"no-store"}});
const safe=v=>String(v??"UNKNOWN").replace(/[^0-9A-Za-z_.:/-]/g,"_").slice(0,180);
const hex64=v=>/^[a-f0-9]{64}$/i.test(String(v||""));
const shardFor=lane=>{const n=Math.trunc(Number(lane));return !Number.isFinite(n)||n<1||n>8?null:n<=2?"lanes-1-2":n<=4?"lanes-3-4":n<=6?"lanes-5-6":"lanes-7-8"};
function auth(request,env){const a=String(env.MAINTENANCE_RUNTIME_E2E_PROBE||""),b=String(request.headers.get("x-maintenance-e2e-probe")||"");if(!a||a.length!==b.length)return false;let d=0;for(let i=0;i<a.length;i++)d|=a.charCodeAt(i)^b.charCodeAt(i);return d===0}
async function runExpert(env){if(!env.EXPERT_CENTER?.fetch)return{ok:false,error:"EXPERT_UNBOUND"};const task={task_id:`route-basic-${crypto.randomUUID()}`,prompt:"Compare two internally consistent approaches to a hypothetical capacity-planning problem, identify the stronger one, and give a concise adversarial judgment. No external facts, tools, or web.",task_domain:"quantitative",task_type:"analysis",complexity:"medium",reasoning_depth:"standard",cost_priority:"economy",model_count:2,rounds:1,max_tokens:160,timeout_seconds:240,cost_mode:"free-first"},c=new AbortController(),t=setTimeout(()=>c.abort(),300000);try{const r=await env.EXPERT_CENTER.fetch(new Request("https://expert.internal/v1/run",{method:"POST",headers:{accept:"application/json","content-type":"application/json"},body:JSON.stringify(task),signal:c.signal})),b=await r.json().catch(()=>null),parts=[...(Array.isArray(b?.experts)?b.experts:[]),...(Array.isArray(b?.judges)?b.judges:[])],receipts=parts.map(p=>({model:String(p?.model||""),provider:String(p?.provider||""),company:String(p?.company||""),lane:Number(p?.meta?.lane||p?.lane||0),route_shard:shardFor(p?.meta?.lane||p?.lane)})),companies=new Set(receipts.map(x=>x.company.toLowerCase()).filter(Boolean)),complete=receipts.length>=2&&receipts.every(x=>x.model&&x.provider&&x.company&&x.route_shard&&Number.isInteger(x.lane)&&x.lane>=1&&x.lane<=8),ok=r.status===200&&b?.ok===true&&b?.status==="completed"&&b?.company_diverse===true&&complete&&companies.size>=2&&b?.route_family==="expert-panel"&&b?.route_registry_schema==="expert-route-registry-v4.2-lane-pair"&&hex64(b?.output_digest);return{ok,http_status:r.status,error:ok?null:safe(b?.error||b?.error_code||"EXPERT_ROUTE_FAILED"),participant_count:receipts.length,company_count:companies.size,receipts,route_family:String(b?.route_family||""),route_registry_schema:String(b?.route_registry_schema||""),output_digest:String(b?.output_digest||"")}}catch(e){return{ok:false,http_status:0,error:e?.name==="AbortError"?"TIMEOUT":safe(e?.message||e)}}finally{clearTimeout(t)}}
export default{async fetch(request,env){if(!auth(request,env))return json({ok:false,error:"NOT_FOUND"},404);const expert=await runExpert(env);return json({ok:expert.ok,stage:"expert-existing-route-basic",expert,secrets_redacted:true},expert.ok?200:502)}};
export class MaintenanceState{constructor(state){this.state=state}async fetch(){return json({ok:true,diagnostic_only:true})}}
