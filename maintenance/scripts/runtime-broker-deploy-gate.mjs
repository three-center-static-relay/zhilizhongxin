#!/usr/bin/env node
import {randomBytes} from "node:crypto";
import {readFileSync} from "node:fs";
import {spawnSync} from "node:child_process";
import {resolve} from "node:path";
import {pathToFileURL} from "node:url";
const SHA_PATTERN=/^[a-f0-9]{40,64}$/i;
function emit(row,stream=process.stdout){stream.write(`${JSON.stringify({...row,secrets_redacted:true})}\n`)}
function safeCode(v){return String(v||"UNKNOWN").replace(/[^0-9A-Za-z_.:-]/g,"_").slice(0,180)}
function run(command,args,{cwd=process.cwd(),env=process.env,stdio,encoding="utf8",maxBuffer=8*1024*1024}={}){const r=spawnSync(command,args,{cwd,env,stdio,encoding,maxBuffer});if(r.error)throw r.error;if(r.status!==0){const e=new Error(`${command.toUpperCase()}_FAILED`);e.exitCode=r.status||1;e.stdout=r.stdout;e.stderr=r.stderr;throw e}return r}
function parseWorkersDevUrl(text){const m=String(text||"").match(/https:\/\/[a-z0-9.-]+\.workers\.dev\b/i);if(!m)throw new Error("WORKERS_DEV_URL_NOT_FOUND");const u=new URL(m[0]);if(u.protocol!=="https:"||!u.hostname.endsWith(".workers.dev"))throw new Error("INVALID_WORKERS_DEV_URL");return`${u.protocol}//${u.host}`}
function packageWranglerVersion(){const p=JSON.parse(readFileSync(resolve(process.cwd(),"package.json"),"utf8")),v=String(p.devDependencies?.wrangler||"");if(!/^\d+\.\d+\.\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(v))throw new Error("EXACT_WRANGLER_VERSION_REQUIRED");return v}
function rollback(version,message){try{run("npx",["--yes",`wrangler@${version}`,"rollback","--message",message],{stdio:"inherit"});emit({ok:true,event:"MAINTENANCE_AUTOMATIC_ROLLBACK_COMPLETE"},process.stderr);return true}catch{emit({ok:false,event:"MAINTENANCE_AUTOMATIC_ROLLBACK_FAILED"},process.stderr);return false}}
function deploy(version,{runtimeProbe=null,writeCanary=false,writeScope=false,modelUniverse=false,routeRefresh=false}={}){const args=["--yes",`wrangler@${version}`,"deploy"];if(runtimeProbe)args.push("--var",`MAINTENANCE_RUNTIME_E2E_PROBE:${runtimeProbe}`);if(writeCanary)args.push("--var","MAINTENANCE_AI_GATEWAY_WRITE_CANARY:1");if(writeScope)args.push("--var","MAINTENANCE_AI_GATEWAY_WRITE_SCOPE_PROBE:1");if(modelUniverse)args.push("--var","MAINTENANCE_MODEL_UNIVERSE_PROBE:1");if(routeRefresh)args.push("--var","MAINTENANCE_EXPERT_ROUTE_REFRESH_PROBE:1");const r=run("npx",args,{encoding:"utf8"}),output=`${r.stdout||""}\n${r.stderr||""}`,url=parseWorkersDevUrl(output);emit({ok:true,event:runtimeProbe?"MAINTENANCE_E2E_CANDIDATE_DEPLOYED":"MAINTENANCE_CLEAN_DEPLOYED",worker_host:new URL(url).host,probe_persisted:Boolean(runtimeProbe),write_canary_enabled:Boolean(writeCanary),write_scope_probe_enabled:Boolean(writeScope),model_universe_probe_enabled:Boolean(modelUniverse),route_refresh_probe_enabled:Boolean(routeRefresh),production_worker:"maintenance-worker"});return{url}}
function verifyWithScript(script,url,probe,failurePrefix){const r=spawnSync(process.execPath,[resolve(process.cwd(),script),url],{cwd:process.cwd(),encoding:"utf8",env:{...process.env,MAINTENANCE_E2E_PROBE_TOKEN:probe},maxBuffer:8*1024*1024});if(r.stdout)process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);if(r.error)return{ok:false,code:`${failurePrefix}_SPAWN_ERROR`};if(r.status!==0){const escaped=failurePrefix.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),m=`${r.stderr||""}\n${r.stdout||""}`.match(new RegExp(`${escaped}:([^\\r\\n]+)`));return{ok:false,code:safeCode(m?.[1]||`${failurePrefix}_EXIT_${r.status??1}`)}}return{ok:true}}
function verifyWrite(url,probe){return verifyWithScript("scripts/runtime-ai-gateway-write-canary-e2e.mjs",url,probe,"AI_GATEWAY_WRITE_CANARY_E2E_FAILED")}
function verifyUniverse(url,probe){return verifyWithScript("scripts/runtime-model-universe-e2e.mjs",url,probe,"MODEL_UNIVERSE_E2E_FAILED")}
function verifyRoutes(url,probe){return verifyWithScript("scripts/runtime-expert-route-refresh-e2e.mjs",url,probe,"EXPERT_ROUTE_REFRESH_E2E_FAILED")}
function main(){
  const branch=String(process.env.WORKERS_CI_BRANCH||""),sha=String(process.env.WORKERS_CI_COMMIT_SHA||"");if(process.env.WORKERS_CI!=="1")throw new Error("WORKERS_CI_REQUIRED");if(branch!=="main")throw new Error("PRODUCTION_BRANCH_REQUIRED");if(!SHA_PATTERN.test(sha))throw new Error("VALID_COMMIT_SHA_REQUIRED");
  const repoRoot=run("git",["rev-parse","--show-toplevel"],{encoding:"utf8"}).stdout.trim(),sharedGate=resolve(repoRoot,"scripts/cloudflare-worker-gate.mjs"),version=packageWranglerVersion();
  emit({ok:true,event:"MAINTENANCE_DYNAMIC_EXPERT_DEPLOY_GATE_START",commit_sha:sha,policy_owner:"admin-worker",route_execution_owner:"maintenance-worker",credential_custodian:"maintenance-worker",cloudflare_credentials_local:true,model_id_pinning:false,future_models_auto_discover:true,model_source_classes:["workers-ai","openrouter","deepseek","huggingface"],production_dynamic_route_mutation:true});
  const baseline=spawnSync(process.execPath,[sharedGate,"maintenance","deploy"],{cwd:process.cwd(),env:process.env,stdio:"inherit"});if(baseline.error||baseline.status!==0){const e=new Error("BASELINE_MAINTENANCE_DEPLOY_FAILED");e.exitCode=baseline.status||1;throw e}
  const probe=randomBytes(32).toString("hex");let candidate;try{candidate=deploy(version,{runtimeProbe:probe,writeCanary:true,modelUniverse:true,routeRefresh:true})}catch(error){rollback(version,"Automatic rollback: dynamic Expert candidate deploy failed");throw error}
  const universe=verifyUniverse(candidate.url,probe);if(!universe.ok){rollback(version,"Automatic rollback: dynamic model universe acceptance failed");const e=new Error(`MODEL_UNIVERSE_CAPABILITY_FAILED:${universe.code}`);e.exitCode=1;throw e}
  emit({ok:true,event:"MODEL_UNIVERSE_CAPABILITY_PASS",commit_sha:sha,model_id_pinning:false,future_models_auto_discover:true});
  const write=verifyWrite(candidate.url,probe);if(!write.ok){rollback(version,"Automatic rollback: AI Gateway route lifecycle canary failed");const e=new Error(`AI_GATEWAY_WRITE_CAPABILITY_FAILED:${write.code}`);e.exitCode=1;throw e}
  emit({ok:true,event:"AI_GATEWAY_WRITE_CAPABILITY_PASS",commit_sha:sha,permission:"ai_gateway_write",temporary_route_created:true,temporary_route_verified:true,temporary_route_deleted:true,model_invoked:false});
  const routes=verifyRoutes(candidate.url,probe);if(!routes.ok){rollback(version,"Automatic rollback: Expert dynamic route-family acceptance failed");const e=new Error(`EXPERT_ROUTE_FAMILY_FAILED:${routes.code}`);e.exitCode=1;throw e}
  emit({ok:true,event:"EXPERT_DYNAMIC_ROUTE_FAMILY_PASS",commit_sha:sha,model_id_pinning:false,future_models_auto_discover:true,production_dynamic_route_mutation:true});
  try{const finalDeploy=deploy(version);emit({ok:true,event:"MAINTENANCE_DYNAMIC_EXPERT_DEPLOY_GATE_PASS",commit_sha:sha,worker_host:new URL(finalDeploy.url).host,probe_persisted:false,production_attestation:true,policy_owner:"admin-worker",route_execution_owner:"maintenance-worker",credential_custodian:"maintenance-worker",model_id_pinning:false,future_models_auto_discover:true,dynamic_route_family_active:true});}catch(error){rollback(version,"Automatic rollback: clean maintenance deploy failed after route-family acceptance");throw error}
}
const invoked=process.argv[1]?pathToFileURL(resolve(process.argv[1])).href:"";if(import.meta.url===invoked){try{main()}catch(error){emit({ok:false,event:"MAINTENANCE_DYNAMIC_EXPERT_DEPLOY_GATE_FAIL",code:safeCode(error?.message||error)},process.stderr);process.exitCode=Number.isInteger(error?.exitCode)?error.exitCode:1}}
