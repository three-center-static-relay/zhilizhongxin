import {handleLangGraphControl} from "./langgraph-control.js";

const TOKEN_SHA256="42b6d885ae008fc044256891469026a443d9ef85f9e577ffd48cb0015c6333b1";
const EXPIRES_AT=Date.parse("2026-08-22T03:05:00.000Z");
const ENDPOINT="/__runtime-canary/adaptive-rerun-v6";
const CANARY_TASK_ID="runtime-canary-adaptive-rerun-v7";
const PRIMARY_EXPERT_TASK_ID="runtime-adaptive-rerun-v7-expert-primary";
const FALLBACK_EXPERT_TASK_ID="runtime-adaptive-rerun-v7-expert-balanced";
const MAX_RESPONSE_BYTES=2*1024*1024;
const PROMPT=`请比较在福州从事以下几种工作的综合优劣：外卖骑手、快递员、网约车司机、朴朴配送员、保安。请根据具体问题具体分析，重点比较收入潜力、收入稳定性、时间自由度、体力负荷、车辆和设备成本、安全及事故风险、平台规则风险、长期可持续性、进入门槛和综合性价比。不要联网，不使用任何工具；如果缺乏实时本地数据，必须明确不确定性，不要编造当前工资数字。最后给出清晰的综合排序，并分别说明什么类型的人更适合哪一种。`;
const json=(body,status=200)=>Response.json(body,{status,headers:{"cache-control":"no-store"}});
const hex=bytes=>[...new Uint8Array(bytes)].map(x=>x.toString(16).padStart(2,"0")).join("");
async function sha256(value){return hex(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(String(value||""))))}
function constantTimeEqual(a,b){a=String(a||"");b=String(b||"");if(!a||a.length!==b.length)return false;let diff=0;for(let i=0;i<a.length;i++)diff|=a.charCodeAt(i)^b.charCodeAt(i);return diff===0}
async function authorized(input){if(Date.now()>EXPIRES_AT)return false;return constantTimeEqual(await sha256(input?.token||""),TOKEN_SHA256)}
function state(env){return env.ADMIN_COORDINATOR.get(env.ADMIN_COORDINATOR.idFromName("global"))}
async function stateCall(env,path,method="GET",data){const init={method,headers:{"content-type":"application/json"}};if(data!==undefined)init.body=JSON.stringify(data);const r=await state(env).fetch(new Request(`https://state.internal${path}`,init));const body=await r.json().catch(()=>({ok:false,error:"STATE_BAD_RESPONSE"}));if(!r.ok)throw Object.assign(new Error(body.error||"STATE_ERROR"),{status:r.status,details:body});return body}
async function release(env,owner){try{await stateCall(env,"/lock/release","POST",{owner})}catch{}}
async function readJsonBounded(response){const declared=Number(response.headers.get("content-length")||0);if(declared>MAX_RESPONSE_BYTES)throw new Error("EXPERT_RESPONSE_TOO_LARGE");if(!response.body)return null;const reader=response.body.getReader(),decoder=new TextDecoder();let bytes=0,text="";while(true){const{done,value}=await reader.read();if(done)break;bytes+=value.byteLength;if(bytes>MAX_RESPONSE_BYTES){try{await reader.cancel()}catch{};throw new Error("EXPERT_RESPONSE_TOO_LARGE")}text+=decoder.decode(value,{stream:true})}text+=decoder.decode();return text?JSON.parse(text):null}
async function expertStatus(env,taskId){if(!env.EXPERT_CENTER?.fetch)return{ok:false,error:"EXPERT_SERVICE_BINDING_UNAVAILABLE"};const r=await env.EXPERT_CENTER.fetch(new Request("https://expert.internal/v1/status",{method:"POST",headers:{"content-type":"application/json",accept:"application/json"},body:JSON.stringify({task_id:taskId})}));const body=await r.json().catch(()=>null);return{http_status:r.status,...(body||{ok:false,error:"EXPERT_STATUS_BAD_JSON"})}}
async function executeExpert(env,taskId,costMode){const requestBody={task_id:taskId,prompt:PROMPT,tools:false,web:false,...(costMode?{cost_mode:costMode}:{})},started=Date.now(),response=await env.EXPERT_CENTER.fetch(new Request("https://expert.internal/v1/run",{method:"POST",headers:{"content-type":"application/json",accept:"application/json"},body:JSON.stringify(requestBody)})),body=await readJsonBounded(response).catch(error=>({ok:false,error:String(error?.message||error)}));return{ok:response.ok&&body?.ok===true,http_status:response.status,elapsed_ms:Date.now()-started,cost_mode_requested:costMode||null,body}}
function chatCompatFailure(attempt){const text=JSON.stringify(attempt?.body||{}).toLowerCase();return !attempt?.ok&&(text.includes("does not support chat completion")||text.includes('"internalcode":2020')||text.includes('"httpcode":501')||text.includes('"code":2020'))}
async function runOnce(env){
  const task={task_id:"runtime-canary-adaptive-rerun-v7-control",goal:PROMPT,constraints:{allowed_centers:["governance","expert"],write_scope:"none",external_web:false,tools:false,production_mutation:false},risk:{max_trust_level:"T2",uncertainty:"medium"},budget:{cost_mode:"balanced",control:"soft-price-performance",hard_spend_cap:false,token_cap:false,length_control:"adaptive-soft"},required_capabilities:["governance.task-planner","expert.deliberation","expert.judgment"],deadline:new Date(Date.now()+10*60*1000).toISOString(),success_criteria:["LangGraph validates the task","semantic profiling is appropriate for an occupational comparison","panel organization is dynamically adapted","AI Gateway Dynamic Routes select models/providers","chat-incompatible candidates recover through a bounded alternate cost branch","a Judge or final synthesis produces the user-facing final answer","tools and web remain disabled","no production mutation occurs"]};
  const laResponse=await handleLangGraphControl(new Request("https://admin.internal/v1/admin/langgraph/run",{method:"POST",headers:{"content-type":"application/json",accept:"application/json"},body:JSON.stringify(task)}),env),la=laResponse?await laResponse.json().catch(()=>null):null;
  if(!laResponse?.ok||la?.ok!==true)return{ok:false,http_status:laResponse?.status||502,stage:"langgraph-validation",langgraph:la,secrets_redacted:true};
  if(!env.EXPERT_CENTER?.fetch)return{ok:false,http_status:503,stage:"expert-execution",error:"EXPERT_SERVICE_BINDING_UNAVAILABLE",secrets_redacted:true};
  const started=Date.now(),primary=await executeExpert(env,PRIMARY_EXPERT_TASK_ID,null);let selected=primary,recovery={attempted:false,reason:null};
  if(chatCompatFailure(primary)){recovery={attempted:true,reason:"CHAT_COMPLETIONS_INCOMPATIBLE",strategy:"remove-free-first-and-retry-balanced-dynamic-route"};selected=await executeExpert(env,FALLBACK_EXPERT_TASK_ID,"balanced")}
  const ok=selected.ok;
  return{ok,http_status:ok?200:(selected.http_status||502),selftest:"runtime-adaptive-rerun-v7-idempotent",stage:ok?"completed":"expert-execution",elapsed_ms:Date.now()-started,langgraph:{ok:true,status:la?.status||null,runtime:la?.runtime||null,runtime_host:la?.runtime_host||null,control_plane:la?.control_plane||null,planner:la?.planner||null,brain_can_command:la?.brain_can_command===true,execution_mode:la?.execution_mode||null},primary_attempt:{ok:primary.ok,http_status:primary.http_status,elapsed_ms:primary.elapsed_ms,cost_mode_requested:primary.cost_mode_requested,error:primary.ok?null:(primary.body?.error||null)},adaptive_recovery:recovery,selected_attempt:{task_id:selected===primary?PRIMARY_EXPERT_TASK_ID:FALLBACK_EXPERT_TASK_ID,ok:selected.ok,http_status:selected.http_status,elapsed_ms:selected.elapsed_ms,cost_mode_requested:selected.cost_mode_requested},expert:selected.body,tools_used:false,web_used:false,production_mutation:false,secrets_redacted:true};
}
export async function handleRuntimeAdaptiveRerunV6(request,env){
  const url=new URL(request.url);if(url.pathname!==ENDPOINT)return null;if(request.method!=="POST")return json({ok:false,error:"NOT_FOUND"},404);
  let input;try{input=await request.json()}catch{return json({ok:false,error:"NOT_FOUND"},404)}
  if(!(await authorized(input)))return json({ok:false,error:"NOT_FOUND"},404);
  if(input?.operation==="status"){const stored=await stateCall(env,`/task/${encodeURIComponent(CANARY_TASK_ID)}`).catch(()=>({task:null}));return json({ok:true,canary_task:stored.task||null,primary_expert:await expertStatus(env,PRIMARY_EXPERT_TASK_ID),fallback_expert:await expertStatus(env,FALLBACK_EXPERT_TASK_ID),expires_at:new Date(EXPIRES_AT).toISOString(),secrets_redacted:true})}
  if(input?.operation==="cleanup"){await stateCall(env,`/task/${encodeURIComponent(CANARY_TASK_ID)}`,"POST",{id:CANARY_TASK_ID,status:"cleaned",result:null,cleaned_at:new Date().toISOString()});return json({ok:true,cleaned:true,secrets_redacted:true})}
  if(input?.operation!=="run")return json({ok:false,error:"INVALID_OPERATION"},400);
  const existing=await stateCall(env,`/task/${encodeURIComponent(CANARY_TASK_ID)}`).catch(()=>({task:null}));if(existing.task?.status==="completed"&&existing.task?.result)return json(existing.task.result,existing.task.result.http_status||200);if(existing.task?.status==="running")return json({ok:true,status:"running",reused:true,secrets_redacted:true},202);
  const owner=`runtime-canary:${CANARY_TASK_ID}`;try{await stateCall(env,"/lock/acquire","POST",{owner,kind:"runtime-canary",ttl_seconds:900})}catch{return json({ok:true,status:"running",reused:true,secrets_redacted:true},202)}
  try{await stateCall(env,`/task/${encodeURIComponent(CANARY_TASK_ID)}`,"POST",{id:CANARY_TASK_ID,status:"running",started_at:new Date().toISOString()});const result=await runOnce(env);await stateCall(env,`/task/${encodeURIComponent(CANARY_TASK_ID)}`,"POST",{id:CANARY_TASK_ID,status:"completed",result,finished_at:new Date().toISOString()});return json(result,result.http_status||500)}catch(error){const result={ok:false,http_status:error?.status||500,stage:"canary",error:String(error?.message||error),secrets_redacted:true};await stateCall(env,`/task/${encodeURIComponent(CANARY_TASK_ID)}`,"POST",{id:CANARY_TASK_ID,status:"completed",result,finished_at:new Date().toISOString()}).catch(()=>{});return json(result,result.http_status)}finally{await release(env,owner)}
}
