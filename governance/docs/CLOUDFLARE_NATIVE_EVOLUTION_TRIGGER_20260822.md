# Cloudflare-native evolution candidate trigger

PR candidate validation trigger after PR creation.

Scope remains Analytics Engine telemetry + existing Workers Observability only. No production mutation, no Secret change, no model/provider policy change.
