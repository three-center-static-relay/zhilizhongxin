# Rollback Controller

Purpose: recover safely when an upgrade reduces system quality.

Triggers:

- increased error rate
- timeout increase
- evaluation regression
- policy violation

Action:

restore previous approved model/runtime configuration.
