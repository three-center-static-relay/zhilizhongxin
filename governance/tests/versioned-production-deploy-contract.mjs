import assert from "node:assert/strict";
import fs from "node:fs";
const source=fs.readFileSync(new URL("../scripts/versioned-production-deploy.mjs",import.meta.url),"utf8");
assert.match(source,/WORKERS_CI_BRANCH,"main"/);
assert.match(source,/versions","upload/);
assert.match(source,/WRANGLER_OUTPUT_FILE_PATH/);
assert.match(source,/deployments","status/);
assert.match(source,/versions","deploy/);
assert.match(source,/\$\{candidate\}@0%/);
assert.match(source,/\$\{previous\}@100%/);
assert.match(source,/Cloudflare-Workers-Version-Overrides/);
assert.match(source,/x-three-center-selftest/);
assert.match(source,/service-binding-fetch/);
assert.match(source,/routes_readable/);
assert.match(source,/dynamic_route_mutation===false/);
assert.match(source,/expert_called===false/);
assert.match(source,/secrets_redacted===true/);
assert.match(source,/GOVERNANCE_VERSIONED_PRODUCTION_ROLLBACK_COMPLETE/);
assert.match(source,/PROMOTION_VERIFY_FAILED/);
assert.doesNotMatch(source,/wrangler\W+deploy\W/);
assert.doesNotMatch(source,/AI_GATEWAY.*create|routes\.create|versions\.create|deployments\.create/i);
console.log(JSON.stringify({ok:true,suite:"governance-versioned-production-deploy-contract",decoupled_versions:true,zero_traffic_smoke:true,version_override:true,service_binding_required:true,automatic_rollback:true,no_dynamic_route_write:true,secrets_redacted:true}));
